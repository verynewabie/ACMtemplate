```cpp
#include <bits/stdc++.h>
#define fi first
#define se second
#define pb push_back
#define mp make_pair
#define all(x) (x).begin(), (x).end()
#define lowbit(x) ((x) & -(x))
#define SZ(x) (int)x.size()
#define V vector
#ifndef LOCAL
#define endl '\n'
#endif
using namespace std;
using ll = long long;
using ld = long double;
using ull = unsigned long long;
using pii = pair<int, int>;
using pll = pair<ll, ll>;
using pdd = pair<ld, ld>;
mt19937 mrand(random_device{}());
int rnd(int x) { return mrand() % x; }
const int INF = 0x3f3f3f3f;
void solve()
{
}
signed main()
{
#ifndef LOCAL
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#endif
#ifdef LOCAL
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
#endif
    int t = 1;
    //cin >> t;
    for (int i = 1; i <= t; i++)
        solve();
    return 0;
}
```

cmake加宏：

![image-20230724115322793](https://cartoonwqy.oss-cn-nanjing.aliyuncs.com/2023/07/868349fccaf0b9dd480cdb21c5e09e93.png)
